/**
 * Frontend type definitions.
 *
 * All types are sourced from @ub-task/shared-types — the single source of truth.
 * No type is redeclared here. Any addition or change to the API shape happens
 * in packages/shared-types/index.ts and propagates to both backend and frontend
 * automatically.
 */
export * from '@ub-task/shared-types';
